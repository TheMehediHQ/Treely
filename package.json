{
  "name": "trelly",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "mongodb": "^6.16.0"
  },
  "scripts": {
    "dev": "bun run --filter '@trelly/client' dev",
    "build": "bun run --filter '@trelly/client' build",
    "dev:server": "node api/index.js"
  }
}
